<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmation</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
            padding: 20px;
        }

        h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
            color: #2c3e50;
        }

        pre {
            background-color: #ffffff;
            border: 1px solid #ddd;
            border-radius: 5px;
            padding: 20px;
            overflow-x: auto;
            font-size: 1em;
            white-space: pre-wrap; /* Allow wrapping */
            word-wrap: break-word; /* Break long words */
            margin-bottom: 30px;
        }

        a {
            display: inline-block;
            padding: 10px 20px;
            background-color: #3498db;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: #2980b9; /* Darker shade on hover */
        }
    </style>
</head>
<body>
    <h1>Booking Confirmation</h1>
    <pre>
        Booking ID: <%= reservationDetails.bookingId %>
        From Station: <%= reservationDetails.fromStation %>
        To Station: <%= reservationDetails.toStation %>
        Travel Class: <%= reservationDetails.travelClass %>
        Date of Travel: <%= reservationDetails.date %>
        Name: <%= reservationDetails.name %>
        Aadhar: <%= reservationDetails.aadhar %>
        Mobile Number: <%= reservationDetails.mobile %>
        Number of Passengers: <%= reservationDetails.numberOfPassengers %>
        Total Amount: ₹<%= reservationDetails.totalAmount %>
        Advance Payment: ₹<%= reservationDetails.advance %>
        Remaining Amount: ₹<%= reservationDetails.remainingAmount %>
    </pre>
    <a href="/">Back to Home</a>
</body>
</html>