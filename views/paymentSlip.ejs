<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Slip</title>
    <style>
        @page {
            size: A4;
            margin: 20mm;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            color: #333;
        }

        .container {
            background: linear-gradient(135deg, #ffffff, #d1eaff);
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            max-width: 800px; /* Adjusted width for A4 */
            margin: auto; /* Center the container */
            padding: 40px;
            text-align: left;
        }

        h1 {
            font-size: 2.5em; /* Increased for prominence */
            margin-bottom: 10px;
            color: #2c3e50;
            text-align: center; 
        }

        h2 {
            font-size: 2em; /* Increased for clarity */
            margin-bottom: 20px;
            color: #0e4c92;
            text-align: center;
        }

        pre {
            background: #eaf3ff;
            padding: 15px;
            border-radius: 5px;
            margin-bottom: 20px;
            font-size: 1.1em;
            line-height: 1.4;
            white-space: pre-wrap; /* Allows wrapping in pre */
            word-wrap: break-word; /* Ensures long words break into new lines */
        }

        button {
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            background-color: #3498db; /* Button main color */
            color: white;
            font-size: 1.2em; /* Larger button text */
            cursor: pointer;
            transition: background-color 0.3s ease;
            display: block; /* Centered horizontally */
            width: 100%; /* Full width */
            margin-top: 20px; /* Spacing above button */
        }

        button:hover {
            background-color: #2980b9; /* Darker shade on hover */
        }

        .passenger-details {
            margin-top: 20px;
            font-size: 1em;
            border-top: 2px solid #3498db; /* Added blue border for separation */
            padding-top: 10px; /* Padding above the passenger details */
        }

        .passenger {
            margin-bottom: 10px;
        }

        .passenger strong {
            color: #2c3e50; /* Darker color for labels */
        }

        /* Additional responsive design tweaks */
        @media print {
            body {
                background-color: white; /* Print on white background */
            }

            .container {
                box-shadow: none; /* Remove shadow when printing */
            }

            button {
                display: none; /* Hide button during printing */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Mata Vaishno Devi Public Charitable Trust</h1>
        <h2>Payment Slip</h2>
        <pre>
            Booking ID: <%= booking.bookingId %>
            Name: <%= booking.name %>
            Total Amount: ₹<%= booking.totalAmount %>
            Advance Payment: ₹<%= booking.advance %>
            Remaining Amount: ₹<%= booking.remainingAmount %>
        </pre>

        <div class="passenger-details">
            <h3>Passenger Information</h3>
            <% booking.passengers.forEach(passenger => { %>
                <div class="passenger">
                    <strong>Name:</strong> <%= passenger.name %>, 
                    <strong>Aadhar:</strong> <%= passenger.aadhar %>, 
                    <strong>Mobile:</strong> <%= passenger.mobile %>, 
                    <strong>Age:</strong> <%= passenger.age %>
                </div>
            <% }); %>
        </div>

        <button onclick="window.print()">Print Payment Slip</button>
    </div>
</body>
</html>